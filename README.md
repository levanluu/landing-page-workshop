# Azure Workshop Landing Page

Simple React landing page for Azure Static Web Apps.

## Run locally

```bash
cd web
npm install
npm run dev
```

## Deploy

Build output is in `dist/` after:

```bash
npm run build
```

Use the `web` folder as the Static Web Apps app location.
